import { mount } from '@vue/test-utils';
import GameResult from '@/components/GameResult.vue';

/*
describe('GameResult.vue', () => {
  it('renders results correctly', () => {
    const categories = [
      {
        name: 'Life partner',
        options: [
          { text: 'Spouse', optOut: false, optIn: true },
          { text: 'Partner', optOut: true, optIn: false }
        ]
      }
    ];

    const wrapper = mount(GameResult, {
      props: { categories }
    });

    expect(wrapper.find('li').text()).toContain('Spouse');
  });

  it('getResult returns correct result for category', () => {
    const categories = [
      {
        name: 'Life partner',
        options: [
          { text: 'Spouse', optOut: false, optIn: true },
          { text: 'Partner', optOut: true, optIn: false }
        ]
      }
    ];

    const wrapper = mount(GameResult, {
      props: { categories }
    });

    expect(wrapper.vm.getResult('Life partner')).toBe('Spouse');
  });
});
*/
